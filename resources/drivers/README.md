# OEM USB 驱动包占位目录

此目录只允许放置触摸屏厂商提供、可重新分发且已签名的 Windows x64 驱动包。

首版不会修改 INF，也不会将通用 WinUSB 强制绑定到未知 USB 接口。获得驱动后，需要同时提供：

- 驱动包中的 `.inf`、`.cat` 和相关文件；
- 适用的 USB VID/PID/MI；
- 驱动版本；
- 厂商名称；
- 重新分发授权说明。

未提供上述资料时，应用只显示驱动排查提示，不提供虚假的“一键万能驱动”。
