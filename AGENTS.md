# AGENTS.md

## UI Test：UI 自动验收

适用范围：涉及 UI 行为的功能开发和缺陷修复。

1. 必须新增或更新可重复运行的 Node.js + Playwright Test 用例，
   保存到项目既有的 E2E 测试目录。
   已有用例充分覆盖时可复用，但必须实际执行。

2. Agent 负责编写测试、分析结果和修复代码；
   浏览器操作与断言检查交给 Playwright runner。
   不使用 browser MCP、Playwright MCP 或 computer use
   逐步操作页面来代替脚本验收。
   允许读取脚本生成的截图、trace 和日志。

3. 通过终端运行项目约定的 Playwright 测试命令。
   不能只生成脚本或阅读代码，就声称验证通过。

4. 修复 bug 时，先让测试因目标业务问题而失败，再修复并重跑。
   服务未启动、登录失败、选择器错误等，不算成功复现目标问题。

5. 测试失败后，根据执行证据定位问题；必要时增加日志、拆解步骤，
   但必须保留原问题的触发条件。
   不得通过删除断言、跳过用例或弱化预期来获得通过。

6. 完成前运行目标用例及相关回归。
   最终报告执行命令、结果和未验证范围。
   环境阻塞或无法复现时如实报告，不得声称修复或验收完成。
